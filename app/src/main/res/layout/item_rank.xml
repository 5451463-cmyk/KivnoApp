<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:gravity="center_vertical">

    <TextView android:id="@+id/txtRank"
        android:layout_width="36dp" android:layout_height="wrap_content"
        android:textSize="18sp" android:textStyle="bold" android:textColor="@color/terra"
        android:fontFamily="sans-serif-black"/>

    <ImageView android:id="@+id/imgRank"
        android:layout_width="44dp" android:layout_height="62dp"
        android:scaleType="centerCrop" android:background="@color/surface"
        android:layout_marginEnd="12dp"/>

    <TextView android:id="@+id/txtRankTitle"
        android:layout_width="0dp" android:layout_height="wrap_content"
        android:layout_weight="1" android:textSize="14sp" android:textStyle="bold"
        android:textColor="@color/dark" android:maxLines="2" android:ellipsize="end"/>

    <TextView android:id="@+id/txtRankPoop"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:textSize="12sp" android:textStyle="bold" android:textColor="@color/poop_light"/>

</LinearLayout>
